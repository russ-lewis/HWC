part main {
    bit[32][64] a;

    auto b = a;
    auto c = a[0];
    auto d = a[1:3];
    auto e = a[2][0];
    auto f = a[3][16:];
    auto g = a[63:];

    auto h = b;
    auto i = e;

    auto j = concat(b,b);

    auto k = concat(concat(b,b), concat(b,b));

    auto l = concat( a[:16], a[16:] );
}

